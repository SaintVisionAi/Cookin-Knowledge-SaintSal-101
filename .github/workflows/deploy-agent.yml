name: 🔁 Deploy via SaintSal Core Agent

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deploy Endpoint
        run: |
          curl -X POST https://cookinknowledge.com/api/deploy.ts \
          -H "x-api-key: 1cb9cbba8c00ad2db02d114ffdbc7385cd9027ee"
